import React from "react";
import PropTypes from "prop-types";

function KegList(props) {
  return ( 
    <React.Fragment>
      <hr />
      {props.}
    </React.Fragment>
  );
}

export default KegList;